"use client"

// import { ServerStructure, useServerModel } from "@/hooks/main-store";

export const InitilizedServer = ({server} : any) => {
    // const {setServerModel} = useServerModel()
    // useServerModel((state: ServerStructure) => server);
    // console.log(useServerModel);
    
  return (
    null
  )
}
